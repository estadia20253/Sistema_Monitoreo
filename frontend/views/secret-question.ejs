<main class="auth-container">
    <h2>❓ Pregunta Secreta</h2>
    <% if (typeof error !== 'undefined') { %>
        <p class="error">❌ <%= error %></p>
    <% } %>
    
    <div class="info-note">
        <p><strong>📌 Cuenta encontrada:</strong> <%= userData.email %></p>
        <p>Responde a tu pregunta secreta para continuar con la recuperación de contraseña.</p>
    </div>

    <form method="POST" action="/verify-secret-answer" class="auth-form">
        <input type="hidden" name="email" value="<%= userData.email %>">
        
        <div class="secret-question-section">
            <label class="question-label">🔐 Tu pregunta secreta:</label>
            <div class="question-display">
                <%= userData.preguntaSecreta %>
            </div>
        </div>
        
        <label>💭 Tu respuesta:
            <input type="text" name="respuestaSecreta" required 
                   placeholder="Ingresa la respuesta exacta que registraste"
                   title="Debe coincidir exactamente con la respuesta que proporcionaste al registrarte">
        </label>
        
        <button type="submit">✅ Verificar Respuesta</button>
        
        <div class="auth-nav-link">
            ¿Quieres intentar con otro email? <a href="/forgot-password">Regresar</a>
        </div>
        
        <div class="auth-nav-link">
            ¿Recordaste tu contraseña? <a href="/login">Inicia sesión aquí</a>
        </div>
    </form>
</main>
